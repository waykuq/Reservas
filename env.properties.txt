# quitar la extensión .txt de este archivo y colocarlo en la carpeta raíz del proyecto

# usuario y password de mysql
DB_USER=root
DB_PASSWORD=root

# nombre de la base de datos
DB_NAME=DB_RESERVAS

# ruta de conexión a la base de datos
DB_URL_PREFIX=jdbc:mysql://localhost:3306/
DB_URL_SUFFIX=?serverTimezone=UTC
